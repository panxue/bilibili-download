# Iteration 0.5.1 — review fixes: queued cancel ineffectiveness + doc drift correction

## Changes

- Queue cancel fix | `_worker` dequeue without state check → skip terminal-state jobs (`status in TERMINAL`) | a job canceled while queued would still start downloading after dequeue = cancel ineffective; now canceled means stopped
- Frontend default quality dropdown | missing 1080P60/720P60/360P → filled in the three tiers with echo fallback | after 0.4.1 grouped quality by qn, the settings panel had no entry to preset high-frame-rate tiers; a value not in the list could not be selected
- api.md proofreading | quality enum adds 1080P60/720P60, removed the misleading "greyed out" sentence, SIGINT→SIGTERM, `-9`→`-409`, progress removed bytes_done/bytes_total | doc aligned with implementation: `_terminate` uses SIGTERM; `_handle` routes illegal status to HTTPException(409); `to_api` progress only percent/speed/eta
- Error-code table narrowed | -4/-5 removed (not implemented in code) | only list codes that actually exist
- config.md/ui.md proofreading | quality enum completed; `overwrite` marked as frontend-fixed false; removed unimplemented claims such as `floatCollapsed`/`floatPinned`/done "re-download"/cross-page persistence/backend-unreachable degraded preset tiers | unimplemented capabilities are not written into the docs

## Implementation notes

- Full review (code + docs + security + git hygiene) per session records: ruff passed, cookie three-key chain/`--cookies`/token -401/SIGTERM all verified; git 33 files, no secrets committed.
- 0.5.0 Docker-side verification conclusions migrated in: BLDLP_TOKEN takes effect on /api/info etc. (health exempt), interrupted `.part` preserved via volume sharing (`--paths temp:/app/data/.parts` + `--continue`).

## TODO / rollback notes

- Rollback: remove the `_worker` check line + restore the frontend dropdown; doc changes have no runtime impact.
- Future: `overwrite` frontend option (currently hardcoded false).
