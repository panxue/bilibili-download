# Iteration 0.5.2 — pytest unit test moat + requirements current-state sync

## Changes

- Test framework | none → `pytest` + `pytest-asyncio` (dev group), `pyproject.toml` adds `[tool.pytest.ini_options]` (pythonpath/testpaths/asyncio_mode=auto) | backend core logic previously had zero automated guarantees, relying on manual testing; no regression defense for later changes
- Unit tests | none → `backend/tests/` (6 files, 46 cases) | covers codec normalization/preference, quality_to_format height and codec chain, parse_probe qn grouping/high-frame-rate distinction/codec sorting, **cancel-queued regression** (0.5.1 fix), cookie pre-validation (valid/invalid/network error/bad JSON), db persistence/cookies not persisted/scan_interrupted
- Mirror source pinned | none → `pyproject.toml` `[[tool.uv.index]]` (Tsinghua PyPI default) | user environment network-constrained, `uv add` auto-pins it, subsequent uv operations default to the Tsinghua source
- requirements.md current-state sync | v0.1 finalized/five preset tiers/cookies not written to disk/Docker excluded/open-folder → mark implemented versions, quality list data-driven by qn, cookie into cookies.txt noted, Docker moved out of exclusions, open-folder moved into exclusions | requirements baseline conflicts in several places with implementation (0.3.0 persistent cookies, 0.4.1 qn grouping, 0.5.0 Docker), docs must be trustworthy

## Implementation notes

- Tests fully offline: no network access (mock `urllib.request`), no real yt-dlp subprocess (async worker uses a replaced `_run` to verify queue behavior).
- db contract exposed by test bugs: `update_status`'s `**fields` treated directly as SQL column names (must pass `params_json` instead of `params`); `scan_interrupted` only marks "pending and not active".
- Commands: `uv run ruff check backend/` + `uv run pytest` (AGENTS.md commands section updated).

## TODO / rollback notes

- Rollback: remove dev-group pytest dependency + `backend/tests/` + pytest config; requirements.md changes have no runtime impact.
- Future: `overwrite` frontend option; open-folder button (explicitly listed as an exclusion).
